{
  "name": "open-stream-environment-streamdeck",
  "version": "1.0.0",
  "private": true,
  "description": "Elgato Stream Deck plugin for Open Stream Environment",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "esbuild app.js --bundle --platform=node --target=node18 --external:bufferutil --external:utf-8-validate --outfile=dist/app.js",
    "icons": "node scripts/generate-icons.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
